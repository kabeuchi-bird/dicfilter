name: Release

on:
  release:
    types: [created]

permissions:
  contents: write

jobs:
  build-release:
    name: ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            bin: dicfilter
            asset: dicfilter-linux-x86_64.zip
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            bin: dicfilter.exe
            asset: dicfilter-windows-x86_64.zip
          - os: macos-latest
            target: aarch64-apple-darwin
            bin: dicfilter
            asset: dicfilter-macos-aarch64.zip
          - os: macos-13
            target: x86_64-apple-darwin
            bin: dicfilter
            asset: dicfilter-macos-x86_64.zip
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Install Linux GUI dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libxcb-render0-dev \
            libxcb-shape0-dev libxcb-xfixes0-dev libxkbcommon-dev libssl-dev

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}

      - name: Package (Unix)
        if: runner.os != 'Windows'
        run: |
          cd target/${{ matrix.target }}/release
          zip ../../../${{ matrix.asset }} ${{ matrix.bin }}

      - name: Package (Windows)
        if: runner.os == 'Windows'
        run: |
          Compress-Archive -Path target/${{ matrix.target }}/release/${{ matrix.bin }} -DestinationPath ${{ matrix.asset }}

      - name: Upload release asset
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ matrix.asset }}
